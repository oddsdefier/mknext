---
"mknext": minor
---

Add a `doctor --update` flag, keep the Claude environment guard on by default and skip it with a report when its tools are missing, add a repository CI workflow. Fix the audit workflow-permissions check, which rejected the valid inline `permissions: {contents: read}` form.
