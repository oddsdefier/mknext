# Changesets

Add one changeset for each user-visible change.

Run:

```bash
pnpm changeset
```

Apply pending versions with:

```bash
pnpm changeset:version
```

The version command also copies the package version to `VERSION`.
