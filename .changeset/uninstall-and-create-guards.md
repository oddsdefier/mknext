---
"mknext": minor
---

Add an `mknext uninstall` command that removes the CLI from its install prefix. Ask for confirmation before the short `mknext <name>` form creates an app. Reject a create target inside the mknext source or install directory.
