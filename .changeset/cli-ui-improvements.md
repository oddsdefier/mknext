---
"mknext": minor
---

Add polished CLI UI with styled headers, step timers, guided prompts, health reports, the `mknext sync` command, extended CI targets (`local` and `github`), and the `mknext audit` command with automated security gates and shell safe install (`--setup-safe-install`) supply chain protection.
